/**
 *
 * choi-trie; trie for choi (based on radix-trie)
 *
 * Author : jbear; JI-WOONG CHOI
 * Contact : http://jbear.co
 * Copyright @ jbear
 *
**/

var ChoiTrie = (function() {
    function ChoiTrie() {
        var about = {
            VERSION : '0.1',
            AUTHOR : "jbear"
        };
        this.root = {};
    }
    ChoiTrie.prototype = {
        add: function(_word, _what) {
        },
        search: function(_word) {
        }
    };

    return ChoiTrie;
}());
